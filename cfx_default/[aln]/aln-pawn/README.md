# aln-pawn (ALN3)

Pawn sell endpoint:
- sells configured items for cash
- supports variant pricing (meta.variant) for loot_valuable
- server authoritative: inventory remove + economy credit

v0 UI:
- simple selector sells 1 at a time (until full inventory UI exists)

Required locations:
- tag `pawn`
